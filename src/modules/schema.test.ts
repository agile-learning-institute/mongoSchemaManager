import { Schema } from './schema';

describe('Schema', () => {
    let schema: Schema;

    beforeAll( () => {
    });

    afterAll(async () => {
    });

    test('test msmType', async () => {
    });

    test('test msmEnum', async () => {
    });

    test('get msmEnumList', async () => {
    });

});