import { Version } from './version';

describe('Version', () => {
    let version: Version;

    beforeAll( () => {
    });

    afterAll(async () => {
    });

    test('test getVersion', async () => {
    });

    test('test applyVersion', async () => {
    });

});